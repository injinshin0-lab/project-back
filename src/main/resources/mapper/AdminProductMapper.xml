<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.kosmo.project_back.admin.mapper.AdminProductMapper">

    <select id="findAllCategories" resultType="kr.co.kosmo.project_back.admin.dto.AdminCategoryDto">
        SELECT
            id AS categoryId,
            category_name AS categoryName,
            parent_id AS parentId,
            depth AS level
        FROM Bg_Interest_category
        ORDER BY depth, id
    </select>

    <select id="countProductList" parameterType="kr.co.kosmo.project_back.admin.dto.ProductSearchDto" resultType="int">
        SELECT COUNT(DISTINCT p.id)
        FROM Bg_Product p
        <choose>
            <when test="categoryIds != null and categoryIds.size() > 0 or categoryId != null">
                INNER JOIN Bg_Category_product_mapping cpm ON p.id = cpm.product_id
            </when>
            <otherwise>
                LEFT JOIN Bg_Category_product_mapping cpm ON p.id = cpm.product_id
            </otherwise>
        </choose>
        <where>
            <if test="keyword != null and keyword != ''">
                (p.product_name LIKE '%' || #{keyword} || '%' 
                     OR p.content LIKE '%' || #{keyword} || '%'
                     OR p.origin_name LIKE '%' || #{keyword} || '%')
            </if>
            <if test="categoryIds != null and categoryIds.size() > 0">
                AND cpm.interest_category_id IN
                <foreach collection="categoryIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="categoryIds == null and categoryId != null">
                AND cpm.interest_category_id = #{categoryId}
            </if>
        </where>
    </select>

    <resultMap id="productResultMap" type="kr.co.kosmo.project_back.admin.dto.AdminProductResponseDto">
        <id property="productId" column="productId"/>
        <result property="productName" column="productName"/>
        <result property="originName" column="originName"/>
        <result property="content" column="content"/>
        <result property="price" column="price"/>
        <result property="imageUrl" column="imageUrl"/>
        <association property="category" javaType="kr.co.kosmo.project_back.admin.dto.AdminCategoryDto">
            <id property="categoryId" column="cat_categoryId"/>
            <result property="categoryName" column="cat_categoryName"/>
            <result property="parentId" column="cat_parentId"/>
            <result property="level" column="cat_level"/>
        </association>
    </resultMap>

    <select id="findProductList" parameterType="kr.co.kosmo.project_back.admin.dto.ProductSearchDto"
            resultMap="productResultMap">
        SELECT
            p.id AS productId,
            p.product_name AS productName,
            p.origin_name AS originName,
            p.content,
            p.price,
            p.image_url AS imageUrl,
            MAX(c.id) AS cat_categoryId,
            MAX(c.category_name) AS cat_categoryName,
            MAX(c.parent_id) AS cat_parentId,
            MAX(c.depth) AS cat_level
        FROM Bg_Product p
        <choose>
            <when test="categoryIds != null and categoryIds.size() > 0 or categoryId != null">
                INNER JOIN Bg_Category_product_mapping cpm ON p.id = cpm.product_id
                LEFT JOIN Bg_Interest_category c ON cpm.interest_category_id = c.id
            </when>
            <otherwise>
                LEFT JOIN Bg_Category_product_mapping cpm ON p.id = cpm.product_id
                LEFT JOIN Bg_Interest_category c ON cpm.interest_category_id = c.id
            </otherwise>
        </choose>
        <where>
            <if test="keyword != null and keyword != ''">
                (p.product_name LIKE '%' || #{keyword} || '%' 
                     OR p.content LIKE '%' || #{keyword} || '%'
                     OR p.origin_name LIKE '%' || #{keyword} || '%')
            </if>
            <if test="categoryIds != null and categoryIds.size() > 0">
                AND cpm.interest_category_id IN
                <foreach collection="categoryIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="categoryIds == null and categoryId != null">
                AND cpm.interest_category_id = #{categoryId}
            </if>
        </where>
        GROUP BY p.id, p.product_name, p.origin_name, p.content, p.price, p.image_url
        <choose>
            <when test="sort != null and sort == 'productName'">
                ORDER BY p.product_name 
                <if test="order != null and order == 'asc'">ASC</if>
                <if test="order == null or order == 'desc'">DESC</if>
            </when>
            <when test="sort != null and sort == 'price'">
                ORDER BY p.price 
                <if test="order != null and order == 'asc'">ASC</if>
                <if test="order == null or order == 'desc'">DESC</if>
            </when>
            <when test="sort != null and (sort == 'id' or sort == 'productId')">
                ORDER BY p.id 
                <if test="order != null and order == 'asc'">ASC</if>
                <if test="order == null or order == 'desc'">DESC</if>
            </when>
            <otherwise>
                ORDER BY p.id DESC
            </otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="findByProductId" parameterType="int"
            resultMap="productResultMap">
        SELECT
            p.id AS productId,
            p.product_name AS productName,
            p.origin_name AS originName,
            p.content,
            p.price,
            p.image_url AS imageUrl,
            c.id AS cat_categoryId,
            c.category_name AS cat_categoryName,
            c.parent_id AS cat_parentId,
            c.depth AS cat_level
        FROM Bg_Product p
        LEFT JOIN Bg_Category_product_mapping cpm ON p.id = cpm.product_id
        LEFT JOIN Bg_Interest_category c ON cpm.interest_category_id = c.id
        WHERE p.id = #{productId}
        LIMIT 1
    </select>

    <insert id="insertProduct" parameterType="kr.co.kosmo.project_back.admin.dto.AdminProductRequestDto"
            useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO Bg_Product (product_name, origin_name, content, price, image_url)
        VALUES (#{productName}, #{originName}, #{content}, #{price}, #{imageUrl})
    </insert>

    <update id="updateProduct" parameterType="kr.co.kosmo.project_back.admin.dto.AdminProductRequestDto">
        UPDATE Bg_Product
        <set>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="originName != null">origin_name = #{originName},</if>
            <if test="content != null">content = #{content},</if>
            <if test="price != null">price = #{price},</if>
            <if test="imageUrl != null and imageUrl != ''">image_url = #{imageUrl}</if>
        </set>
        WHERE id = #{productId}
    </update>

    <delete id="deleteProduct" parameterType="int">
        DELETE FROM Bg_Product WHERE id = #{productId}
    </delete>

</mapper>

