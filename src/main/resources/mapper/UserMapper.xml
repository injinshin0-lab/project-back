<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.kosmo.project_back.user.mapper.UserMapper">

    <!-- 회원가입 -->
    <insert id="insertUser"
            parameterType="kr.co.kosmo.project_back.user.vo.UserVO"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO bg_user
        (login_id, password, name, phone, email, zipcode, address, address_detail )
        VALUES
        (#{loginId}, #{password}, #{name}, #{phone}, #{email}, #{zipcode}, #{address}, #{addressDetail})
    </insert>
    
    <!-- 아이디로 회원조회 -->
    <select id="selectByLoginId"
            parameterType="string"
            resultType="kr.co.kosmo.project_back.user.vo.UserVO">
        SELECT
            id,
            login_id        AS loginId,
            password,
            name,
            phone,
            email,
            role,
            created_at      AS createdAt,
            updated_at      AS updatedAt,
            last_login_at   AS lastLoginAt
        FROM bg_user
        WHERE login_id = #{loginId}
    </select>

    <!-- 아이디 중복 체크 -->
    <select id="existsByLoginId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM bg_user
        WHERE login_id = #{loginId}
    </select>

    <!-- 자동로그인용: id로 회원 조회 -->
    <select id="selectById"
            parameterType="int"
            resultType="kr.co.kosmo.project_back.user.vo.UserVO">
        SELECT
            id,
            login_id        AS loginId,
            password,
            name,
            phone,
            email,
            role,
            created_at      AS createdAt,
            updated_at      AS updatedAt,
            last_login_at   AS lastLoginAt
        FROM bg_user
        WHERE id = #{id}
    </select>

</mapper>
