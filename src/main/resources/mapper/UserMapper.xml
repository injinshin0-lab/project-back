<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.kosmo.project_back.user.mapper.UserMapper">

    <!-- 회원가입 -->
    <insert id="insertUser"
            parameterType="kr.co.kosmo.project_back.user.dto.UserJoinDto"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO Bg_User
        (login_id, password, user_name, phone, email)
        VALUES
        (#{loginId}, #{password}, #{name}, #{phone}, #{email})
    </insert>
    
    <!-- 아이디로 회원조회 -->
    <select id="selectByLoginId"
            parameterType="string"
            resultType="kr.co.kosmo.project_back.user.dto.UserDto">
        SELECT
            id,
            login_id        AS loginId,
            password,
            user_name       AS userName,
            phone,
            email,
            role,
            created_at      AS createdAt,
            updated_at      AS updatedAt,
            last_login_at   AS lastLoginAt
        FROM Bg_User
        WHERE login_id = #{loginId}
    </select>

    <!-- 아이디 중복 체크 -->
    <select id="existsByLoginId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM Bg_User
        WHERE login_id = #{loginId}
    </select>

    <!-- 자동로그인용 : id로 회원 조회 -->
    <select id="selectById"
            parameterType="int"
            resultType="kr.co.kosmo.project_back.user.dto.UserDto">
        SELECT
            id,
            login_id        AS loginId,
            password,
            user_name       AS userName,
            phone,
            email,
            role,
            created_at      AS createdAt,
            updated_at      AS updatedAt,
            last_login_at   AS lastLoginAt
        FROM Bg_User
        WHERE id = #{id}
    </select>

    <!-- 아이디 찾기 -->
    <select id="findByNameAndEmail" resultType="kr.co.kosmo.project_back.user.dto.UserDto">
        SELECT 
            id,
            login_id        AS loginId,
            email
        FROM Bg_User WHERE user_name = #{name} AND email = #{email}
    </select>

    <!-- 바뀐 비번 저장 -->
    <update id="updatePasswordByEmail">
        UPDATE Bg_User
        SET password = #{password},
            updated_at = CURRENT_TIMESTAMP
        WHERE email = #{email}
    </update>

    <!-- 마지막 로그인 시간 업뎃 -->
    <update id="updateLastLoginAt">
        UPDATE Bg_User 
        SET last_login_at = CURRENT_TIMESTAMP
        WHERE id = #{userId} 
    </update>

    <!-- 마이페이지 - 회원 정보 수정 -->
    <update id="updateUserInfo">
        UPDATE Bg_User
        <set>
            <if test="dto.userName != null">
                user_name = #{dto.userName},
            </if>
            <if test="dto.phone != null">
                phone = #{dto.phone},
            </if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{userId} 
    </update>

    <!-- 마이페이지 비번 변경 -->
     <update id="updatePasswordByUserId">
        UPDATE Bg_User
        SET password = #{password},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{userId}
     </update>

</mapper>
