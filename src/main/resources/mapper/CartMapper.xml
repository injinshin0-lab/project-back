<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.kosmo.project_back.cart.mapper.CartMapper">

    <!-- 장바구니 항목 조회 -->
    <select id="findCartListByUserId" resultType="kr.co.kosmo.project_back.cart.dto.CartDto">
        SELECT 
            c.id              AS cartItemId,
            c.product_id      AS productId,
            p.product_name    AS productName,
            c.quantity,
            p.price
        FROM Bg_Cart c
        JOIN Bg_Product p ON c.product_id = p.id 
        WHERE c.user_id = #{userId}        
    </select>

    <!-- 장바구니 추가 -->
    <insert id="insertCartItem">
        INSERT INTO Bg_Cart (user_id, product_id, quantity)
        VALUES (#{userId}, #{productId}, #{quantity})
    </insert>

    <!-- 수량 수정 -->
    <update id="updateCartItemByCartItemId"> 
        UPDATE Bg_Cart
        SET quantity = #{quantity}
        WHERE id = #{cartItemId}
            AND user_id = #{userId}
    </update>

    <!-- 장바구니 항목 삭제(단건) -->
    <delete id="deleteCartItem">
        DELETE FROM Bg_Cart
        WHERE user_id = #{userId}
            AND product_id = #{productId}
    </delete>


    <!-- 장바구니 전체 삭제 -->
    <delete id="deleteCart">
        DELETE FROM Bg_Cart
        WHERE user_id = #{userId}
    </delete>

</mapper>


<!-- updateCartQuantity를 추가한 이유... -->
<!-- 1. insertOrUpdateCartItem 하나로 장바구니 추가/수정을 
전부 처리하려고 했으나... 그러기 위해선 DB에 user_id/product_id가
UNIQUE/PK여야 함..
2. DB를 그대로 둔다면..
UNIQUE 제약이 없기 때문에 중복 여부를 판단할수 없음..
그래서 수정.추가를 한번에 구현하지 못한다.. -->






