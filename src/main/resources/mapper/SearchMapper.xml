<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.kosmo.project_back.user.mapper.SearchMapper">

    <insert id="insertSearchHistory">
        INSERT INTO Bg_Search_history (user_id, keyword, searched_at)
        VALUES (#{userId}, #{keyword}, datetime('now'))
    </insert>

    <select id="findPopularKeywords" resultType="string">
        SELECT keyword
        FROM Bg_Search_history
        WHERE searched_at >= datetime('now', '-30 days')
        GROUP BY keyword
        ORDER BY COUNT(*) DESC
        LIMIT 10
    </select>

    <select id="findRecentKeywordsByUserId" parameterType="int" resultType="string">
        SELECT DISTINCT keyword
        FROM Bg_Search_history
        WHERE user_id = #{userId}
        ORDER BY searched_at DESC
        LIMIT 10
    </select>

</mapper>

